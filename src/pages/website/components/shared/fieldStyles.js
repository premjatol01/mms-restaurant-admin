// Class strings shared by the custom inputs in this module (built from the existing theme classes).
export const inputClass =
  "w-full px-3 py-2 text-sm rounded-lg bg-surface text-theme border border-theme placeholder:text-gray-400 focus-ring-primary";

export const labelClass = "block text-sm font-medium text-theme mb-1";

export const checkboxClass = "w-4 h-4 rounded border-theme accent-primary";

// Smaller variant for dense rows (for example gallery captions).
export const compactInputClass =
  "w-full px-2.5 py-1.5 text-xs rounded-lg bg-surface text-theme border border-theme placeholder:text-gray-400 focus-ring-primary";
